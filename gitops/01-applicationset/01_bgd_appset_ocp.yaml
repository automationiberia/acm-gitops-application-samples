---
apiVersion: argoproj.io/v1alpha1
kind: ApplicationSet
metadata:
  name: appset-bgd-ocp
  namespace: openshift-gitops
spec:
  generators:
    - matrix:
        generators:
          - clusterDecisionResource:
              configMapRef: acm-placement
              labelSelector:
                matchLabels:
                  cluster.open-cluster-management.io/placement: app-gitops-placement-ocp
              requeueAfterSeconds: 180
          - list:
              elements:
                - env: dev-ocp
  template:
    metadata:
      name: "app-bootstrap-bgd-{{ name }}-{{ env }}"
    spec:
      ignoreDifferences:
        - kind: ServiceAccount
          jsonPointers:
            - /imagePullSecrets
            - /secrets
      project: acm
      syncPolicy:
        automated:
          prune: true
          selfHeal: true
        syncOptions:
        - CreateNamespace=true
      source:
        repoURL: "https://github.com/automationiberia/acm-gitops-application-samples.git"
        targetRevision: "demo"
        path: "applications/bgd/overlays/{{ env }}"
      destination:
        namespace: "{{ env }}-bgd"
        server: "{{ server }}"
...
